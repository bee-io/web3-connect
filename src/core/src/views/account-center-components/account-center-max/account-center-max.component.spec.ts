import { TestBed } from '@angular/core/testing';
import { AccountCenterMaxComponent } from './account-center-max.component';

describe('AccountCenterMaxComponent', () => {
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [
        AccountCenterMaxComponent
      ],
    }).compileComponents();
  });
});
