import { TestBed } from '@angular/core/testing';
import { AccountCenterMinComponent } from './account-center-min.component';

describe('AccountCenterMinComponent', () => {
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [
        AccountCenterMinComponent
      ],
    }).compileComponents();
  });
});
