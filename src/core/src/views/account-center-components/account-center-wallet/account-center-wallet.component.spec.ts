import { TestBed } from '@angular/core/testing';
import { AccountCenterWalletComponent } from './account-center-wallet.component';

describe('AccountCenterWalletComponent', () => {
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [
        AccountCenterWalletComponent
      ],
    }).compileComponents();
  });
});
