import { TestBed } from '@angular/core/testing';
import { ConnectedWalletComponent } from './connected-wallet.component';
describe('ConnectedWalletComponent', () => {
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [
        ConnectedWalletComponent
      ],
    }).compileComponents();
  });
});
