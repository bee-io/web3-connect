import { TestBed } from '@angular/core/testing';
import { ConnectingWalletComponent } from './connecting-wallet.component';
describe('ConnectingWalletComponent', () => {
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [
        ConnectingWalletComponent
      ],
    }).compileComponents();
  });
});
