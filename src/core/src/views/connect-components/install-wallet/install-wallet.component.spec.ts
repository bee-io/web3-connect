import { TestBed } from '@angular/core/testing';
import { InstallWalletComponent } from './install-wallet.component';
describe('InstallWalletComponent', () => {
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [
        InstallWalletComponent
      ],
    }).compileComponents();
  });
});
