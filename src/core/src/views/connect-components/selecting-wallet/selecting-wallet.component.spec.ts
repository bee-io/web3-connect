import { TestBed } from '@angular/core/testing';
import { SelectingWalletComponent } from './selecting-wallet.component';
describe('SelectingWalletComponent', () => {
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [
        SelectingWalletComponent
      ],
    }).compileComponents();
  });
});
