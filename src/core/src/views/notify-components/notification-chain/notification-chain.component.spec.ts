import { TestBed } from '@angular/core/testing';
import { NotificationChainComponent } from './notification-chain.component';

describe('NotificationChainComponent', () => {
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [
        NotificationChainComponent
      ],
    }).compileComponents();
  });
});
