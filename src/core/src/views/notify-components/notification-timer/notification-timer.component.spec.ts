import { TestBed } from '@angular/core/testing';
import { NotificationTimerComponent } from './notification-timer.component';

describe('NotificationTimerComponent', () => {
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [
        NotificationTimerComponent
      ],
    }).compileComponents();
  });
});
