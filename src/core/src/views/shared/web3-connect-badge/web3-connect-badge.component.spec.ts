import { TestBed } from '@angular/core/testing';
import { Web3ConnectBadgeComponent } from './web3-connect-badge.component';

describe('Web3ConnectBadgeComponent', () => {
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [
        Web3ConnectBadgeComponent
      ],
    }).compileComponents();
  });
});
