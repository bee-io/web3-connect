import { TestBed } from '@angular/core/testing';
import { Web3ConnectSpinnerComponent } from './web3-connect-spinner.component';

describe('Web3ConnectSpinnerComponent', () => {
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [
        Web3ConnectSpinnerComponent
      ],
    }).compileComponents();
  });
});
