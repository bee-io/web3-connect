import { TestBed } from '@angular/core/testing';
import { Web3ConnectWarningComponent } from './web3-connect-warning.component';

describe('Web3ConnectWarningComponent', () => {
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [
        Web3ConnectWarningComponent
      ],
    }).compileComponents();
  });
});
